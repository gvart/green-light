**./application-[ENV].yml**: contains all shared configuration. In order to override them just redeclare property for 
concrete service configuration file.


**-[ENV]** extension will be used with active [ENV] spring profile