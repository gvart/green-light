spring:
  application:
    name: event-service
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:8888


  data:
    mongodb:
      username: mongo
      password: password
      database: event_service
      port: 27017
      host: localhost
      authentication-database: admin
